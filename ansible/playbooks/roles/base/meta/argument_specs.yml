---
argument_specs:
  main:
    short_description: Validate base role variables

    options:
      base_upgrade_enabled:
        type: bool
        required: false
        default: true
        description: Whether the base role should run system package upgrades
