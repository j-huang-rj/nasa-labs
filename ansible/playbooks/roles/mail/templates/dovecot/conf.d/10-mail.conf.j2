# Dovecot mail location configuration - managed by Ansible

# Maildir storage under shared vmail path
mail_location = maildir:{{ mail_vmail_home }}/%n/Maildir

# Namespace configuration
namespace inbox {
 inbox = yes
}
