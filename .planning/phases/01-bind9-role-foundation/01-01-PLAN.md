---
phase: 01-bind9-role-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/playbooks/roles/bind9/defaults/main.yml
  - ansible/playbooks/roles/bind9/meta/argument_specs.yml
  - ansible/playbooks/roles/bind9/tasks/main.yml
  - ansible/playbooks/roles/bind9/tasks/assert.yml
  - ansible/playbooks/roles/bind9/tasks/setup.yml
autonomous: true
requirements:
  - AUTO-01
must_haves:
  truths:
    - Operator can drive one bind9 role from validated inventory inputs instead of forking host-specific roles.
    - Invalid bind9 role inputs fail before any package install or service mutation starts.
  artifacts:
    - path: ansible/playbooks/roles/bind9/defaults/main.yml
      provides: bind9 package, service, and filesystem defaults.
    - path: ansible/playbooks/roles/bind9/meta/argument_specs.yml
      provides: validated bind9 mode and listen-address schema.
    - path: ansible/playbooks/roles/bind9/tasks/main.yml
      provides: role entrypoint following repository conventions.
    - path: ansible/playbooks/roles/bind9/tasks/assert.yml
      provides: runtime validation before setup begins.
    - path: ansible/playbooks/roles/bind9/tasks/setup.yml
      provides: phase orchestration anchor for later install, config, and service work.
  key_links:
    - from: ansible/playbooks/bootstrap.yml
      to: ansible/playbooks/roles/bind9/tasks/main.yml
      via: role include gated by bind9_enabled
      pattern: "role: bind9"
    - from: ansible/playbooks/roles/bind9/meta/argument_specs.yml
      to: ansible/playbooks/roles/bind9/tasks/assert.yml
      via: validated role variables consumed by runtime asserts
      pattern: "bind9_mode|bind9_listen_ipv4"
---

<objective>
Create the bind9 role contract and entrypoint scaffolding that every later Phase 1 change can build against.

Purpose: Establish one data-driven component role that matches repository conventions and rejects bad inventory early.
Output: `defaults/main.yml`, `meta/argument_specs.yml`, `tasks/main.yml`, `tasks/assert.yml`, and `tasks/setup.yml` for `ansible/playbooks/roles/bind9/`.
</objective>

<execution_context>
@/Users/j.huang.rj/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/j.huang.rj/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-bind9-role-foundation/01-RESEARCH.md
@lab/dns.md
@ansible/playbooks/bootstrap.yml
@ansible/inventory/group_vars/all.yml
@ansible/playbooks/roles/network/tasks/main.yml
@ansible/playbooks/roles/network/tasks/assert.yml
@ansible/playbooks/roles/network/meta/argument_specs.yml
@ansible/playbooks/roles/firewall/meta/argument_specs.yml

<interfaces>
From `ansible/playbooks/bootstrap.yml`:
```yaml
- role: bind9
  when: bind9_enabled
```

From `ansible/playbooks/roles/network/tasks/main.yml`:
```yaml
- name: "START"
  become: false
  ansible.builtin.debug:
    msg: "Starting execution of network role"

- name: Validate Network Role Variables
  ansible.builtin.import_tasks: assert.yml

- name: Configure Network Interfaces
  ansible.builtin.import_tasks: setup.yml
```

From `ansible/playbooks/roles/network/meta/argument_specs.yml`:
```yaml
argument_specs:
  main:
    short_description: Validate network role variables
    options:
      network_interfaces:
        type: list
        elements: dict
        required: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define bind9 defaults and role schema</name>
  <read_first>
ansible/playbooks/roles/network/meta/argument_specs.yml
ansible/playbooks/roles/firewall/meta/argument_specs.yml
ansible/inventory/group_vars/all.yml
  </read_first>
  <files>ansible/playbooks/roles/bind9/defaults/main.yml, ansible/playbooks/roles/bind9/meta/argument_specs.yml</files>
  <action>Create `defaults/main.yml` and `meta/argument_specs.yml` for the `bind9` component role. Follow the existing `network` and `firewall` schema style: `argument_specs.main.options` with explicit types, defaults, and choices. Define defaults for `bind9_packages` with `bind` and `bind-utils`, `bind9_service_name` with `named`, `bind9_conf_path` with `/etc/named.conf`, `bind9_conf_dir` with `/etc/named`, `bind9_zone_dir` with `/var/named`, `bind9_dynamic_zone_dir` with `/var/named/dynamic`, `bind9_config_validate_enabled` with `true`, `bind9_runtime_verify_enabled` with `true`, and `bind9_identity_source_host` with `router-01`. Require `bind9_mode` with choices `authoritative_primary`, `authoritative_secondary`, and `resolver`, plus required `bind9_listen_ipv4` as a non-empty list of IPv4 strings. Keep variable names generic and role-scoped; do not create host-specific variable names and do not duplicate WireGuard secrets into tracked files.</action>
  <acceptance_criteria>
- `ansible/playbooks/roles/bind9/defaults/main.yml` contains `bind9_packages:` with `bind` and `bind-utils`.
- `ansible/playbooks/roles/bind9/defaults/main.yml` contains `bind9_service_name: named`.
- `ansible/playbooks/roles/bind9/meta/argument_specs.yml` contains `bind9_mode` with choices `authoritative_primary`, `authoritative_secondary`, and `resolver`.
- `ansible/playbooks/roles/bind9/meta/argument_specs.yml` contains `bind9_listen_ipv4` as a required list.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "bind9_packages|bind9_service_name|bind9_mode|bind9_listen_ipv4" ansible/playbooks/roles/bind9/defaults/main.yml ansible/playbooks/roles/bind9/meta/argument_specs.yml</automated>
  </verify>
  <done>Bind9 defaults and argument specs exist and define one inventory contract for primary, secondary, and resolver hosts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the bind9 stub with component-role scaffolding</name>
  <read_first>
ansible/playbooks/roles/bind9/tasks/main.yml
ansible/playbooks/roles/network/tasks/main.yml
ansible/playbooks/roles/network/tasks/assert.yml
ansible/playbooks/roles/network/tasks/setup.yml
  </read_first>
  <files>ansible/playbooks/roles/bind9/tasks/main.yml, ansible/playbooks/roles/bind9/tasks/assert.yml, ansible/playbooks/roles/bind9/tasks/setup.yml</files>
  <action>Replace the stub `ansible/playbooks/roles/bind9/tasks/main.yml` with the repository-standard `START` to `assert.yml` to `setup.yml` to `END` flow. Create `tasks/assert.yml` with `PHASE [assert : ...]` task names and fail-fast checks that `bind9_enabled | bool` is true when the role runs, `bind9_mode` is one of `authoritative_primary`, `authoritative_secondary`, or `resolver`, and `bind9_listen_ipv4` is defined and non-empty. Create `tasks/setup.yml` with `PHASE [setup : START]` and `PHASE [setup : END]` markers only; Plan 02 will extend it with install, config, and service imports. Use `ansible.builtin.assert` with `quiet: true` and actionable `fail_msg` strings, matching existing role style.</action>
  <acceptance_criteria>
- `ansible/playbooks/roles/bind9/tasks/main.yml` contains `ansible.builtin.import_tasks: assert.yml` and `ansible.builtin.import_tasks: setup.yml`.
- `ansible/playbooks/roles/bind9/tasks/assert.yml` contains `PHASE [assert : START]` and `PHASE [assert : END]`.
- `ansible/playbooks/roles/bind9/tasks/assert.yml` contains assertions for `bind9_mode` and `bind9_listen_ipv4`.
- `ansible/playbooks/roles/bind9/tasks/setup.yml` contains `PHASE [setup : START]` and `PHASE [setup : END]`.
  </acceptance_criteria>
  <verify>
    <automated>ansible-playbook -i ansible/inventory/hosts.example.yml ansible/playbooks/bootstrap.yml --syntax-check</automated>
  </verify>
  <done>The bind9 role is no longer a TODO stub and now matches the repository component-role entrypoint pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory → bind9 role | Untrusted inventory values decide how `named` will be configured on live hosts. |
| controller → managed host | Generated config crosses from the control node into privileged target paths under `/etc` and `/var/named`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `meta/argument_specs.yml` and `tasks/assert.yml` | mitigate | Restrict `bind9_mode` to the three allowed choices and require explicit `bind9_listen_ipv4` input before setup begins. |
| T-01-02 | E | `tasks/main.yml` entrypoint | mitigate | Gate all later privileged work behind `bind9_enabled | bool` and fail-fast assertions so the role cannot proceed from malformed inventory. |
</threat_model>

<verification>
- `ansible-playbook -i ansible/inventory/hosts.example.yml ansible/playbooks/bootstrap.yml --syntax-check`
- `rg -n "bind9_mode|bind9_listen_ipv4|bind9_service_name|PHASE \[assert :" ansible/playbooks/roles/bind9`
</verification>

<success_criteria>
- The bind9 role has a real entrypoint instead of a TODO stub.
- One validated schema covers primary, secondary, and resolver host modes.
- Invalid bind9 inventory inputs are rejected before any install or service tasks are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bind9-role-foundation/01-01-SUMMARY.md`
</output>
