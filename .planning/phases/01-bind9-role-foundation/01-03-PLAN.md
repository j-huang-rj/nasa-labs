---
phase: 01-bind9-role-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - ansible/inventory/group_vars/all.yml
  - ansible/inventory/host_vars/primary-ns-01/main.yml
  - ansible/inventory/host_vars/secondary-ns-01/main.yml
  - ansible/inventory/host_vars/dns-01/main.yml
  - ansible/playbooks/roles/bind9/tasks/config.yml
  - ansible/playbooks/roles/bind9/tasks/setup.yml
  - ansible/playbooks/roles/bind9/tasks/verify.yml
  - ansible/playbooks/roles/bind9/templates/named.conf.j2
  - ansible/playbooks/roles/bind9/templates/named.acl.conf.j2
autonomous: true
requirements:
  - AUTO-01
  - AUTH-01
  - SEC-01
  - RES-01
must_haves:
  truths:
    - `lab_id: 14` and DNS host mode data come from inventory rather than hardcoded template literals.
    - The bind9 role derives the VPN subnet input at runtime from existing inventory data instead of copying secret values into tracked files.
    - Running the bind9 role on the three DNS hosts ends with `named` enabled, active, and listening on each host IP.
  artifacts:
    - path: ansible/inventory/group_vars/all.yml
      provides: shared `lab_id` input.
    - path: ansible/inventory/host_vars/primary-ns-01/main.yml
      provides: authoritative primary bind9 mode and listen IP.
    - path: ansible/inventory/host_vars/secondary-ns-01/main.yml
      provides: authoritative secondary bind9 mode and listen IP.
    - path: ansible/inventory/host_vars/dns-01/main.yml
      provides: resolver bind9 mode and listen IP.
    - path: ansible/playbooks/roles/bind9/templates/named.acl.conf.j2
      provides: ACL fragment using DMZ, internal, and derived VPN CIDRs.
    - path: ansible/playbooks/roles/bind9/tasks/verify.yml
      provides: syntax, service-state, and listen-socket verification.
  key_links:
    - from: ansible/inventory/group_vars/all.yml
      to: ansible/playbooks/roles/bind9/tasks/config.yml
      via: `lab_id` consumed during runtime identity derivation
      pattern: "lab_id"
    - from: ansible/inventory/host_vars/router-01/secrets.yml
      to: ansible/playbooks/roles/bind9/tasks/config.yml
      via: `hostvars[bind9_identity_source_host].wireguard_address`
      pattern: "wireguard_address"
    - from: ansible/playbooks/roles/bind9/tasks/verify.yml
      to: ansible/inventory/host_vars/primary-ns-01/main.yml
      via: `bind9_listen_ipv4` checked with `ss -ltnu`
      pattern: "ss -ltnu"
---

<objective>
Wire inventory-derived identity into the bind9 role and add the verification path that proves the role works on all three DNS hosts.

Purpose: Close the phase goal by making `lab_id`, host mode, and VPN subnet inputs come from inventory data and by proving `named` starts cleanly on the target hosts.
Output: Updated inventory vars, a runtime ACL template, and `verify.yml` imported by the bind9 role.
</objective>

<execution_context>
@/Users/j.huang.rj/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/j.huang.rj/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-bind9-role-foundation/01-RESEARCH.md
@.planning/phases/01-bind9-role-foundation/01-02-PLAN.md
@ansible/inventory/group_vars/all.yml
@ansible/inventory/host_vars/primary-ns-01/main.yml
@ansible/inventory/host_vars/secondary-ns-01/main.yml
@ansible/inventory/host_vars/dns-01/main.yml
@ansible/inventory/hosts.example.yml
@ansible/inventory/host_vars/router-01/secrets.example.yml

<interfaces>
From `ansible/inventory/host_vars/primary-ns-01/main.yml`:
```yaml
network_interfaces:
  - ifname: eth0
    zone: internal
    ip4: 172.16.1.53/24
bind9_enabled: true
```

From `ansible/inventory/host_vars/secondary-ns-01/main.yml`:
```yaml
wireguard_enabled: true
bind9_enabled: true
```

From `ansible/inventory/host_vars/router-01/secrets.example.yml`:
```yaml
# wireguard_address: 192.168.0.0/28
```

From `ansible/inventory/hosts.example.yml`:
```yaml
dmz:
  hosts:
    secondary-ns-01:
      ansible_host: 172.16.0.53

internal:
  hosts:
    primary-ns-01:
      ansible_host: 172.16.1.53
    dns-01:
      ansible_host: 172.16.1.153
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inventory inputs for lab identity and bind9 host modes</name>
  <read_first>
ansible/inventory/group_vars/all.yml
ansible/inventory/host_vars/primary-ns-01/main.yml
ansible/inventory/host_vars/secondary-ns-01/main.yml
ansible/inventory/host_vars/dns-01/main.yml
  </read_first>
  <files>ansible/inventory/group_vars/all.yml, ansible/inventory/host_vars/primary-ns-01/main.yml, ansible/inventory/host_vars/secondary-ns-01/main.yml, ansible/inventory/host_vars/dns-01/main.yml</files>
  <action>Add `lab_id: 14` to `ansible/inventory/group_vars/all.yml`. In each DNS host var file, keep `bind9_enabled: true` and add explicit bind9 inputs: `primary-ns-01` gets `bind9_mode: authoritative_primary` and `bind9_listen_ipv4: [172.16.1.53]`; `secondary-ns-01` gets `bind9_mode: authoritative_secondary` and `bind9_listen_ipv4: [172.16.0.53]`; `dns-01` gets `bind9_mode: resolver` and `bind9_listen_ipv4: [172.16.1.153]`. Do not duplicate `wireguard_address`, private keys, or any other secret values into tracked inventory files.</action>
  <acceptance_criteria>
- `ansible/inventory/group_vars/all.yml` contains `lab_id: 14`.
- `ansible/inventory/host_vars/primary-ns-01/main.yml` contains `bind9_mode: authoritative_primary` and `bind9_listen_ipv4:` with `172.16.1.53`.
- `ansible/inventory/host_vars/secondary-ns-01/main.yml` contains `bind9_mode: authoritative_secondary` and `bind9_listen_ipv4:` with `172.16.0.53`.
- `ansible/inventory/host_vars/dns-01/main.yml` contains `bind9_mode: resolver` and `bind9_listen_ipv4:` with `172.16.1.153`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "lab_id: 14|bind9_mode:|bind9_listen_ipv4:|172.16.1.53|172.16.0.53|172.16.1.153" ansible/inventory/group_vars/all.yml ansible/inventory/host_vars/primary-ns-01/main.yml ansible/inventory/host_vars/secondary-ns-01/main.yml ansible/inventory/host_vars/dns-01/main.yml</automated>
  </verify>
  <done>Tracked inventory now provides the bind9 role with shared lab identity and explicit per-host runtime mode and listen inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Derive VPN identity at runtime and add final bind9 verification</name>
  <read_first>
ansible/playbooks/roles/bind9/tasks/config.yml
ansible/playbooks/roles/bind9/tasks/setup.yml
ansible/playbooks/roles/bind9/templates/named.conf.j2
ansible/inventory/host_vars/router-01/secrets.example.yml
  </read_first>
  <files>ansible/playbooks/roles/bind9/tasks/config.yml, ansible/playbooks/roles/bind9/tasks/setup.yml, ansible/playbooks/roles/bind9/tasks/verify.yml, ansible/playbooks/roles/bind9/templates/named.conf.j2, ansible/playbooks/roles/bind9/templates/named.acl.conf.j2</files>
  <action>Update `tasks/config.yml` so it computes runtime identity inputs from inventory before rendering templates. Read `hostvars[bind9_identity_source_host].wireguard_address`, split the IPv4 string into octets, compute `bind9_vpn_network_base = (octet4 // 16) * 16`, rebuild `bind9_vpn_network_cidr` as `octet1.octet2.octet3.bind9_vpn_network_base/28`, and compute `bind9_derived_lab_id = (octet3 * 16) + (bind9_vpn_network_base // 16)`. Assert that `lab_id == bind9_derived_lab_id` when `wireguard_address` is available. Create `templates/named.acl.conf.j2` and update `templates/named.conf.j2` to include it; the ACL file must render `acl "bind9_dmz_clients" { 172.16.0.0/24; };`, `acl "bind9_internal_clients" { 172.16.1.0/24; };`, and `acl "bind9_vpn_clients" { {{ bind9_vpn_network_cidr }}; };`. Create `tasks/verify.yml` and import it from `tasks/setup.yml` after `service.yml`. In `verify.yml`, run `named-checkconf /etc/named.conf`, `systemctl is-enabled named`, `systemctl is-active named`, and an `ss -ltnu` check that each `bind9_listen_ipv4` address is bound on port 53. Add a final shell check that `grep -R "\${ID}" /etc/named.conf /etc/named/*.conf` returns no matches. Use `changed_when: false` for every verification command.</action>
  <acceptance_criteria>
- `ansible/playbooks/roles/bind9/tasks/config.yml` contains `bind9_vpn_network_cidr` and `bind9_derived_lab_id` derivation logic.
- `ansible/playbooks/roles/bind9/templates/named.acl.conf.j2` contains `acl "bind9_dmz_clients"`, `acl "bind9_internal_clients"`, and `acl "bind9_vpn_clients"`.
- `ansible/playbooks/roles/bind9/tasks/setup.yml` contains `ansible.builtin.import_tasks: verify.yml`.
- `ansible/playbooks/roles/bind9/tasks/verify.yml` contains `named-checkconf /etc/named.conf`, `systemctl is-active named`, and `ss -ltnu`.
  </acceptance_criteria>
  <verify>
    <automated>ansible-playbook -i ansible/inventory/hosts.example.yml ansible/playbooks/bootstrap.yml --syntax-check</automated>
  </verify>
  <done>The bind9 role now consumes inventory-driven `lab_id`, derives VPN identity from existing inventory data, renders ACL scaffolding, and has an automated verification phase for syntax, service state, and listening sockets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secret inventory hostvars → bind9 runtime facts | Gitignored WireGuard data informs runtime-derived VPN CIDRs used by tracked templates. |
| bind9 verification → deployment confidence | Automated checks decide whether the daemon is safe to keep enabled on live hosts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | I | `tasks/config.yml` identity derivation | mitigate | Consume `wireguard_address` from existing inventory hostvars at runtime and assert `lab_id` matches the derived subnet formula instead of copying VPN data into tracked files or hardcoding `${ID}`. |
| T-01-06 | D | `tasks/verify.yml` | mitigate | Verify `named-checkconf`, `systemctl is-enabled`, `systemctl is-active`, and `ss -ltnu` on every DNS host before declaring the plan complete. |
</threat_model>

<verification>
- `ansible-playbook -i ansible/inventory/hosts.yml ansible/playbooks/bootstrap.yml --limit primary-ns-01,secondary-ns-01,dns-01`
- `ansible -i ansible/inventory/hosts.yml primary-ns-01,secondary-ns-01,dns-01 -m ansible.builtin.command -a "named-checkconf /etc/named.conf"`
- `ansible -i ansible/inventory/hosts.yml primary-ns-01,secondary-ns-01,dns-01 -m ansible.builtin.shell -a "systemctl is-enabled named && systemctl is-active named && ss -ltnu | grep -E "(:53\b)""`
- `ansible -i ansible/inventory/hosts.yml primary-ns-01,secondary-ns-01,dns-01 -m ansible.builtin.shell -a "grep -R "\${ID}" /etc/named.conf /etc/named/*.conf"`
</verification>

<success_criteria>
- `lab_id: 14` is sourced from inventory and no bind9 template relies on a hardcoded `${ID}` literal.
- The bind9 role derives VPN subnet input from inventory at runtime without copying secrets into tracked files.
- `named` is enabled, active, and listening on `172.16.1.53`, `172.16.0.53`, and `172.16.1.153` after the playbook runs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bind9-role-foundation/01-03-SUMMARY.md`
</output>
