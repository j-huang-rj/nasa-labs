---
phase: 03-secured-updates-secondary-replica
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - ansible/inventory/host_vars/primary-ns-01/main.yml
  - ansible/playbooks/roles/bind9/tasks/assert.yml
  - ansible/playbooks/roles/bind9/tasks/config.yml
  - ansible/playbooks/roles/bind9/tasks/verify.yml
  - ansible/playbooks/roles/bind9/templates/named.zones.conf.j2
autonomous: true
requirements:
  - AUTH-07
  - AUTH-08
must_haves:
  truths:
    - "D-05, D-06, D-07, D-08, D-09: Signed updates to the primary succeed only for `dynamic1-4.<ID>.nasa` A records and approved private PTR records."
    - "Unsigned, out-of-policy, and non-secondary transfer attempts against the primary are rejected."
    - "D-10, D-11: The primary announces and exposes transfers only to the secondary replica address `172.16.0.53`."
  artifacts:
    - path: ansible/inventory/host_vars/primary-ns-01/main.yml
      provides: Dynamic-update host list and zone-local transfer/notify policy data
      contains: bind9_dynamic_hosts
    - path: ansible/playbooks/roles/bind9/templates/named.zones.conf.j2
      provides: Zone-level `update-policy`, `allow-transfer`, and `also-notify` rendering
      contains: update-policy
    - path: ansible/playbooks/roles/bind9/tasks/verify.yml
      provides: Primary-side signed update and rejection probes
      contains: nsupdate -k /etc/named/named.keys.conf
  key_links:
    - from: ansible/inventory/host_vars/primary-ns-01/main.yml
      to: ansible/playbooks/roles/bind9/templates/named.zones.conf.j2
      via: zone-local `dynamic_update_scope`, `allow_transfer`, and `also_notify` fields
      pattern: bind9_dynamic_hosts|allow_transfer|also_notify|dynamic_update_scope
    - from: ansible/playbooks/roles/bind9/templates/named.zones.conf.j2
      to: ansible/playbooks/roles/bind9/tasks/verify.yml
      via: rendered primary policy that `nsupdate` and AXFR checks exercise
      pattern: update-policy|allow-transfer|also-notify
---

<objective>
Make the primary authoritative service safely writable, tightly scoped, and replication-aware by adding the exact dynamic update and transfer policy required for the lab.

Purpose: Phase 3 fails if the primary either accepts too much, rejects the allowed updates, or never notifies the secondary. This plan turns the live primary into the secure source of truth.
Output: Primary inventory policy data, writable private-zone SELinux handling, rendered `update-policy` / `allow-transfer` / `also-notify`, and primary-side verification for accepted and rejected operations.
</objective>

<execution_context>
@/Users/j.huang.rj/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/j.huang.rj/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-secured-updates-secondary-replica/03-CONTEXT.md
@.planning/phases/03-secured-updates-secondary-replica/03-RESEARCH.md
@.planning/phases/03-secured-updates-secondary-replica/03-VALIDATION.md
@lab/dns.md
@ansible/inventory/host_vars/primary-ns-01/main.yml
@ansible/playbooks/roles/bind9/tasks/assert.yml
@ansible/playbooks/roles/bind9/tasks/config.yml
@ansible/playbooks/roles/bind9/tasks/verify.yml
@ansible/playbooks/roles/bind9/templates/named.zones.conf.j2

<interfaces>
Current zone entry shape in `ansible/inventory/host_vars/primary-ns-01/main.yml`:
```yaml
bind9_zones:
  - name: "{{ bind9_forward_zone_name }}"
    view: private
    type: master
    file: "private/db.{{ bind9_forward_zone_name }}"
```

Current zone renderer capabilities in `ansible/playbooks/roles/bind9/templates/named.zones.conf.j2`:
```jinja2
zone "{{ zone.name }}" IN {
    type {{ zone.type }};
    file "{{ zone.file }}";
{% if zone.masters is defined %}
    masters { {{ zone.masters | join('; ') }}; };
{% endif %}
};
```

Current primary-zone render path in `ansible/playbooks/roles/bind9/tasks/config.yml`:
```yaml
- name: "PHASE [config : Render zone data files]"
  when: bind9_mode == 'authoritative_primary'
  ansible.builtin.template:
    src: db.zone.j2
    dest: "{{ bind9_zone_dir }}/{{ item.file }}"
    setype: "named_zone_t"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact primary-side update, transfer, and notify policy data</name>
  <files>ansible/inventory/host_vars/primary-ns-01/main.yml</files>
  <read_first>.planning/phases/03-secured-updates-secondary-replica/03-CONTEXT.md, .planning/phases/03-secured-updates-secondary-replica/03-RESEARCH.md, ansible/inventory/host_vars/primary-ns-01/main.yml</read_first>
  <action>Update `ansible/inventory/host_vars/primary-ns-01/main.yml` per D-05 through D-11. Add the exact top-level list `bind9_dynamic_hosts: ["dynamic1", "dynamic2", "dynamic3", "dynamic4"]`. Do **not** pre-create `dynamic1-4` A records in static zone data; verification must prove live creation through `nsupdate`. Extend the existing master-zone entries so the private forward zone carries `dynamic_update_scope: forward_a_hosts`, the private `0.16.172.in-addr.arpa` zone carries `dynamic_update_scope: ptr_zonesub`, and the private `1.16.172.in-addr.arpa` zone carries `dynamic_update_scope: ptr_zonesub`. Add `allow_transfer: ["172.16.0.53"]` and `also_notify: ["172.16.0.53"]` to every primary master zone that the secondary must mirror. Do not add `dynamic_update_scope` to the public forward zone or the public VPN reverse child zone, and do not add any `allow-update` key anywhere.</action>
  <acceptance_criteria>
- `ansible/inventory/host_vars/primary-ns-01/main.yml` contains `bind9_dynamic_hosts:` with exactly `dynamic1`, `dynamic2`, `dynamic3`, and `dynamic4`.
- The private forward-zone entry contains `dynamic_update_scope: forward_a_hosts`.
- The private `0.16.172.in-addr.arpa` and `1.16.172.in-addr.arpa` entries contain `dynamic_update_scope: ptr_zonesub`.
- Primary master-zone entries contain `allow_transfer: ["172.16.0.53"]` and `also_notify: ["172.16.0.53"]`.
- No zone entry introduces `allow-update`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "bind9_dynamic_hosts|dynamic_update_scope|allow_transfer|also_notify|172\.16\.0\.53|allow-update" ansible/inventory/host_vars/primary-ns-01/main.yml</automated>
  </verify>
  <done>The primary inventory now states exactly which names may change dynamically, which reverse zones allow PTR updates, and which replica address may transfer/receive NOTIFY.</done>
</task>

<task type="auto">
  <name>Task 2: Render secure primary policy and prove allowed vs rejected operations</name>
  <files>ansible/playbooks/roles/bind9/tasks/assert.yml, ansible/playbooks/roles/bind9/tasks/config.yml, ansible/playbooks/roles/bind9/templates/named.zones.conf.j2, ansible/playbooks/roles/bind9/tasks/verify.yml</files>
  <read_first>.planning/phases/03-secured-updates-secondary-replica/03-CONTEXT.md, .planning/phases/03-secured-updates-secondary-replica/03-RESEARCH.md, ansible/playbooks/roles/bind9/tasks/assert.yml, ansible/playbooks/roles/bind9/tasks/config.yml, ansible/playbooks/roles/bind9/tasks/verify.yml, ansible/playbooks/roles/bind9/templates/named.zones.conf.j2, ansible/inventory/host_vars/primary-ns-01/main.yml</read_first>
  <action>Implement the role changes that enforce D-06 through D-11. In `ansible/playbooks/roles/bind9/tasks/assert.yml`, add an authoritative-primary validation block that requires `bind9_dynamic_hosts | sort == ['dynamic1', 'dynamic2', 'dynamic3', 'dynamic4']` and rejects any `dynamic_update_scope` on non-private zones. In `ansible/playbooks/roles/bind9/tasks/config.yml`, keep public zone directories/files on `named_zone_t` but render private authoritative zone storage with `named_cache_t` so named can write journals and live updates under SELinux. In `ansible/playbooks/roles/bind9/templates/named.zones.conf.j2`, render optional `allow-transfer`, `also-notify`, and `update-policy` clauses per zone: for `dynamic_update_scope: forward_a_hosts`, emit exact grants `grant lab_ddns_shared name dynamic1.{{ bind9_forward_zone_name }}. A;` through `dynamic4`; for `dynamic_update_scope: ptr_zonesub`, emit `grant lab_ddns_shared zonesub PTR;`; never render `update-policy` on public zones or the VPN reverse child per D-07 and D-08. In `ansible/playbooks/roles/bind9/tasks/verify.yml`, add primary-side probes using `nsupdate -k /etc/named/named.keys.conf`: (1) create `dynamic1.{{ bind9_forward_zone_name }}` A = `172.16.1.200`, (2) modify it to `172.16.1.201`, (3) add `200.1.16.172.in-addr.arpa PTR dynamic1.{{ bind9_forward_zone_name }}.`, (4) delete both records, and then assert the success path is accepted. Add rejection checks proving an unsigned update fails, a signed update to `router.{{ bind9_forward_zone_name }}` fails, and `dig -b 127.0.0.1 @127.0.0.1 {{ bind9_forward_zone_name }} AXFR` is refused because only `172.16.0.53` is in `allow-transfer`. Keep probe-only commands `changed_when: false` and assert refusal by non-zero rc or `REFUSED` / `NOTAUTH` output.</action>
  <acceptance_criteria>
- `ansible/playbooks/roles/bind9/tasks/assert.yml` asserts the exact `dynamic1-4` list and blocks dynamic policy on non-private zones.
- `ansible/playbooks/roles/bind9/tasks/config.yml` renders private authoritative zone storage with `named_cache_t` while public storage remains `named_zone_t`.
- `ansible/playbooks/roles/bind9/templates/named.zones.conf.j2` contains `update-policy`, `allow-transfer`, and `also-notify` rendering branches.
- `ansible/playbooks/roles/bind9/tasks/verify.yml` contains signed success-path `nsupdate -k /etc/named/named.keys.conf` probes plus unsigned/out-of-policy/AXFR rejection checks.
- The rendered policy never introduces `allow-update`.
  </acceptance_criteria>
  <verify>
    <automated>ansible-playbook --syntax-check -i ansible/inventory/hosts.yml ansible/playbooks/dns.yml &amp;&amp; rg -n "dynamic1|dynamic2|dynamic3|dynamic4|update-policy|allow-transfer|also-notify|named_cache_t|nsupdate -k /etc/named/named.keys.conf|REFUSED|NOTAUTH|AXFR" ansible/playbooks/roles/bind9/tasks/assert.yml ansible/playbooks/roles/bind9/tasks/config.yml ansible/playbooks/roles/bind9/templates/named.zones.conf.j2 ansible/playbooks/roles/bind9/tasks/verify.yml</automated>
  </verify>
  <done>The primary now enforces the exact allowed dynamic changes, rejects unauthorized operations, and exposes transfer/NOTIFY only to the secondary replica address.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `nsupdate` client -> primary authoritative zone policy | Untrusted update requests attempt to modify authoritative data |
| primary authoritative service -> AXFR/NOTIFY consumers | Private zone contents must replicate only to the intended secondary endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | E | Primary `update-policy` rendering | mitigate | Emit exact `dynamic1-4` A grants and private-zone `zonesub PTR` grants only; assert the list is exact and never fall back to `allow-update`. |
| T-03-04 | I | Primary zone transfer scope | mitigate | Render `allow-transfer { 172.16.0.53; };` only on primary master zones and add an AXFR refusal probe from loopback to prove non-secondary sources cannot transfer data. |
</threat_model>

<verification>
- `ansible-playbook --syntax-check -i ansible/inventory/hosts.yml ansible/playbooks/dns.yml`
- `rg -n "bind9_dynamic_hosts|dynamic_update_scope|172\.16\.0\.53" ansible/inventory/host_vars/primary-ns-01/main.yml`
- `rg -n "update-policy|allow-transfer|also-notify|lab_ddns_shared|nsupdate -k /etc/named/named.keys.conf|REFUSED|NOTAUTH|AXFR|named_cache_t" ansible/playbooks/roles/bind9/tasks/assert.yml ansible/playbooks/roles/bind9/tasks/config.yml ansible/playbooks/roles/bind9/templates/named.zones.conf.j2 ansible/playbooks/roles/bind9/tasks/verify.yml`
</verification>

<success_criteria>
- Only the private forward zone and private reverse zones render `update-policy`, and the grants are limited to `dynamic1-4` A plus private PTR updates.
- The primary renders `allow-transfer` and `also-notify` only for `172.16.0.53`.
- Verification proves signed allowed changes succeed while unsigned, broader, and non-secondary transfer attempts fail.
</success_criteria>

<output>
After completion, create `.planning/phases/03-secured-updates-secondary-replica/03-02-SUMMARY.md`
</output>
