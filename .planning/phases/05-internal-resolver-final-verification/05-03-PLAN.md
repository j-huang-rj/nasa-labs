---
phase: 05-internal-resolver-final-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - ansible/playbooks/dns_e2e.yml
autonomous: true
requirements:
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - RES-06
must_haves:
  truths:
    - DMZ and Private agents can resolve lab zones and Internet names through `172.16.1.153` using the same path the grader will use.
    - End-to-end checks still prove DNSSEC AD-bit validation, dynamic-update propagation, and DS extraction from the live authoritative service.
    - A second full DNS converge reports `changed=0`, proving grading-readiness idempotency across the DNS stack.
  artifacts:
    - path: ansible/playbooks/dns_e2e.yml
      provides: Cross-host grading-readiness playbook for resolver, authoritative, propagation, and idempotency checks
      contains: import_playbook: dns.yml
  key_links:
    - from: ansible/playbooks/dns_e2e.yml
      to: ansible/playbooks/dns.yml
      via: top-level import followed by end-to-end assertions
      pattern: import_playbook: dns.yml
    - from: ansible/playbooks/dns_e2e.yml
      to: dmz-agent-01 / internal-agent-01 / router-01 / primary-ns-01
      via: delegated dig, nsupdate, and idempotency commands
      pattern: dmz-agent-01|internal-agent-01|router-01|dnssec-dsfromkey -f -
---

<objective>
Create the final grading-readiness playbook that exercises resolver behavior from real client hosts, rechecks propagation and DNSSEC artifacts end to end, and enforces consecutive-run idempotency.

Purpose: Phase 5 is only complete when the full DNS stack passes from the same host perspectives and rerun conditions the Online Judge will observe.
Output: `ansible/playbooks/dns_e2e.yml` importing `dns.yml` and running cross-host resolver, DNSSEC, propagation, ACL, DS, and idempotency assertions.
</objective>

<execution_context>
@/Users/j.huang.rj/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/j.huang.rj/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-internal-resolver-final-verification/05-CONTEXT.md
@.planning/phases/03-secured-updates-secondary-replica/03-03-SUMMARY.md
@.planning/phases/04-authoritative-dnssec-trust-chain/04-02-SUMMARY.md
@lab/dns.md
@ansible/playbooks/site.yml
@ansible/playbooks/dns.yml
@ansible/inventory/hosts.example.yml

<interfaces>
Top-level import pattern from `ansible/playbooks/site.yml`:
```yaml
- name: DNS Services
  ansible.builtin.import_playbook: dns.yml
```

Inventory groups and hostnames from `ansible/inventory/hosts.example.yml`:
```yaml
dmz:
  hosts:
    dmz-agent-01:
    secondary-ns-01:
internal:
  hosts:
    internal-agent-01:
    primary-ns-01:
    dns-01:
dns:
  hosts:
    primary-ns-01:
    secondary-ns-01:
    dns-01:
```

Established verification pattern from `.planning/phases/03-secured-updates-secondary-replica/03-03-SUMMARY.md`:
```text
Propagation test uses dynamic2 A + matching PTR record, polling secondary with until/retries:10/delay:1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the cross-host resolver and blocked-client verification matrix</name>
  <read_first>
    - .planning/phases/05-internal-resolver-final-verification/05-CONTEXT.md
    - .planning/phases/04-authoritative-dnssec-trust-chain/04-02-SUMMARY.md
    - lab/dns.md
    - ansible/playbooks/site.yml
    - ansible/playbooks/dns.yml
    - ansible/inventory/hosts.example.yml
  </read_first>
  <files>
    - ansible/playbooks/dns_e2e.yml
  </files>
  <action>Create `ansible/playbooks/dns_e2e.yml` with a top-level `ansible.builtin.import_playbook: dns.yml`, then add cross-host plays that exercise the resolver from the real graded perspectives. Add a play covering `dmz-agent-01` and `internal-agent-01` that runs `dig @172.16.1.153 private-ns.{{ hostvars['primary-ns-01'].bind9_forward_zone_name }} A +short`, `dig @172.16.1.153 -x 172.16.1.53 +short`, `dig @172.16.1.153 google.com A`, `dig @172.16.1.153 {{ hostvars['primary-ns-01'].bind9_forward_zone_name }} SOA +dnssec`, and `dig @172.16.1.153 {{ hostvars['primary-ns-01'].bind9_vpn_child_reverse_zone_name }} SOA +dnssec`, asserting the private-view answers and `ad` flag from both agent hosts. Add a router-host play that probes `dig -b {{ wireguard_address.split('/')[0] }} @172.16.1.153 google.com A` and treats `REFUSED`, `connection timed out`, or `no servers could be reached` as the expected blocked-client result from the VPN side. Keep every probe `changed_when: false` and fail with the captured stdout/stderr when expectations are not met.</action>
  <acceptance_criteria>
    - `ansible/playbooks/dns_e2e.yml` starts by importing `dns.yml`.
    - `ansible/playbooks/dns_e2e.yml` contains plays for both `dmz-agent-01` and `internal-agent-01` using `@172.16.1.153` queries.
    - `ansible/playbooks/dns_e2e.yml` contains AD-bit checks for both `{{ hostvars['primary-ns-01'].bind9_forward_zone_name }} SOA +dnssec` and `{{ hostvars['primary-ns-01'].bind9_vpn_child_reverse_zone_name }} SOA +dnssec`.
    - `ansible/playbooks/dns_e2e.yml` contains a router-side blocked-client probe using `wireguard_address.split('/')[0]`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from pathlib import Path; text=Path('ansible/playbooks/dns_e2e.yml').read_text(); assert 'import_playbook: dns.yml' in text; assert 'dmz-agent-01' in text and 'internal-agent-01' in text; assert '@172.16.1.153' in text; assert \"wireguard_address.split('/')[0]\" in text"</automated>
  </verify>
  <done>The new end-to-end playbook now exercises resolver behavior from both agent hosts and from a blocked outside-the-zone source.</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end propagation, DS extraction, and consecutive-run idempotency gates</name>
  <read_first>
    - .planning/phases/05-internal-resolver-final-verification/05-CONTEXT.md
    - .planning/phases/03-secured-updates-secondary-replica/03-03-SUMMARY.md
    - .planning/phases/04-authoritative-dnssec-trust-chain/04-02-SUMMARY.md
    - ansible/playbooks/dns_e2e.yml
    - ansible/playbooks/dns.yml
  </read_first>
  <files>
    - ansible/playbooks/dns_e2e.yml
  </files>
  <action>In the same `ansible/playbooks/dns_e2e.yml`, add a primary-host propagation block per D-15 that creates `dynamic4.{{ bind9_forward_zone_name }}` = `172.16.1.204` plus its matching PTR via `nsupdate -k /etc/named/named.keys.conf`, then polls `dig @172.16.1.153 dynamic4.{{ bind9_forward_zone_name }} A +short` from both `dmz-agent-01` and `internal-agent-01` with `until`, `retries: 10`, and `delay: 1`, and always deletes the test A/PTR records afterward. Add DS extraction assertions on `primary-ns-01` using the exact lab pipeline `dig @172.16.1.53 <zone> DNSKEY | dnssec-dsfromkey -f - <zone>` for `{{ bind9_forward_zone_name }}`, `{{ bind9_vpn_child_reverse_zone_name }}`, `0.16.172.in-addr.arpa`, and `1.16.172.in-addr.arpa`, asserting every line contains ` IN DS ` and ` 13 2 `. Finish with a localhost-delegated idempotency task that runs `ANSIBLE_CONFIG=ansible/ansible.cfg ansible-playbook -i ansible/inventory/hosts.yml ansible/playbooks/dns.yml`, then asserts the recap reports `changed=0 failed=0` for `primary-ns-01`, `secondary-ns-01`, and `dns-01`. Do not weaken the idempotency gate by grepping comments; inspect the actual recap line or parse stdout directly.</action>
  <acceptance_criteria>
    - `ansible/playbooks/dns_e2e.yml` contains `dynamic4.{{ bind9_forward_zone_name }}` propagation polling with `retries: 10` and `delay: 1`.
    - `ansible/playbooks/dns_e2e.yml` contains `dnssec-dsfromkey -f -` extraction for the four unique zone names.
    - `ansible/playbooks/dns_e2e.yml` contains a localhost command running `ansible-playbook -i ansible/inventory/hosts.yml ansible/playbooks/dns.yml`.
    - `ansible/playbooks/dns_e2e.yml` asserts `changed=0` and `failed=0` for `primary-ns-01`, `secondary-ns-01`, and `dns-01` from the rerun recap.
  </acceptance_criteria>
  <verify>
    <automated>ANSIBLE_CONFIG=ansible/ansible.cfg ansible-playbook ansible/playbooks/dns_e2e.yml</automated>
  </verify>
  <done>The final playbook proves the resolver path, authoritative DNSSEC artifacts, dynamic-update propagation, and full-stack idempotency from the same host perspectives used in grading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DMZ / Private agents / router → resolver | Real client hosts exercise the graded query paths and must not silently bypass ACL or DNSSEC expectations |
| primary authoritative service → resolver / secondary / end-to-end playbook | Live DNSKEY, DS, and dynamic-update data cross hosts and must remain consistent through propagation |
| localhost idempotency rerun → full DNS stack | The final gate re-enters the provisioning workflow and must detect drift rather than masking it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | R | `ansible/playbooks/dns_e2e.yml` client-perspective checks | mitigate | Run resolver probes from `dmz-agent-01`, `internal-agent-01`, and router-01 instead of only from dns-01 so grading-path regressions are observable in automation. |
| T-05-03-02 | T | `ansible/playbooks/dns_e2e.yml` propagation / DS checks | mitigate | Re-run dynamic-update propagation through the resolver path and extract DS records from live `dig` responses, not local files, so stale authoritative artifacts cannot pass end-to-end verification. |
| T-05-03-03 | D | `ansible/playbooks/dns_e2e.yml` idempotency gate | mitigate | Re-run `dns.yml` and parse the actual recap for `changed=0 failed=0` on all DNS hosts so hidden drift or restart loops fail before submission. |
</threat_model>

<verification>
- `python3 -c "from pathlib import Path; text=Path('ansible/playbooks/dns_e2e.yml').read_text(); assert 'dnssec-dsfromkey -f -' in text; assert 'dynamic4.' in text; assert 'changed=0' in text and 'primary-ns-01' in text and 'secondary-ns-01' in text and 'dns-01' in text"`
- `ANSIBLE_CONFIG=ansible/ansible.cfg ansible-playbook ansible/playbooks/dns_e2e.yml`
</verification>

<success_criteria>
- The final playbook proves resolver correctness from DMZ, Private, and blocked VPN perspectives.
- The final playbook rechecks DS extraction and dynamic-update propagation through the live stack.
- The final playbook enforces `changed=0 failed=0` on a second DNS converge.
</success_criteria>

<output>
After completion, create `.planning/phases/05-internal-resolver-final-verification/05-03-SUMMARY.md`
</output>
