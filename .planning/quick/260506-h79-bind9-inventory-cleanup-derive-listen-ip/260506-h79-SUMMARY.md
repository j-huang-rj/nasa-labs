---
phase: quick-260506-h79-bind9-inventory-cleanup-derive-listen-ip
plan: 01
subsystem: ansible/bind9
tags: [inventory, bind9, DRY, argument_specs, package_ownership]
key_files:
  created: []
  modified:
    - ansible/inventory/group_vars/all.yml
    - ansible/playbooks/roles/bind9/tasks/config.yml
    - ansible/playbooks/roles/base/tasks/install.yml
    - ansible/playbooks/roles/bind9/meta/argument_specs.yml
decisions:
  - Preserved bind9_derived_lab_id fact in config.yml set_fact block for potential future use; only removed the assertion that compared it to lab_id.
  - Kept section comment change from "DNS & Connectivity" to "Connectivity" in base role install.yml to reflect removal of bind-utils.
metrics:
  duration_minutes: 3
  completed: "2026-05-06"
---

# Phase Quick Plan 01: Bind9 Inventory Cleanup & Derive Listen IP Summary

Remove stale bind9 inventory coupling and duplicate default ownership so Phase 1 foundation stays inventory-driven, DRY, and explicit about named listen addresses.

## Tasks Completed

| Task | Name | Commit | Files Modified |
|------|------|--------|---------------|
| 1 | Remove stale lab identity coupling without changing explicit listen-IP ownership | e032e97 | `all.yml`, `config.yml` |
| 2 | Consolidate bind-utils package ownership under the bind9 role | beafdef | `install.yml` (base) |
| 3 | Replace literal argument-spec defaults with Jinja references | 8d0d54a | `argument_specs.yml` (bind9) |

## Verification Results

- `ansible-playbook --syntax-check`: Passed
- `ansible-lint`: 15 pre-existing violations (missing newlines, command-instead-of-module, key-order, line-length, risky-shell-pipe) — none introduced by this plan
- `lab_id` absent from `group_vars/all.yml`: Confirmed
- `bind-utils` absent from base role `install.yml`: Confirmed
- `bind9_vpn_network_cidr` still present in `config.yml` and `named.acl.conf.j2`: Confirmed
- Explicit `bind9_listen_ipv4` values unchanged in all three host_vars: Confirmed
- All 9 non-mandatory argument-spec defaults use Jinja references: Confirmed

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — all mitigations from the threat model were applied as specified.

## Self-Check: PASSED

- All 4 modified files exist on disk
- All 3 task commits verified in git log (e032e97, beafdef, 8d0d54a)
- No accidental file deletions in any commit