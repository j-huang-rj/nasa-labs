# Quick Task 260506-h79: bind9 inventory cleanup - Context

**Gathered:** 2026-05-06
**Status:** Ready for planning

<domain>
## Task Boundary

Four cleanup items for the bind9 role and inventory:
1. Keep `bind9_listen_ipv4` explicit in host_vars (do NOT auto-derive from `network_interfaces[].ip4`)
2. Remove `lab_id: 14` from `group_vars/all.yml` and remove the assertion in `config.yml` that checks `lab_id == bind9_derived_lab_id`
3. Remove `bind-utils` from `base/tasks/install.yml` since it's already in `bind9/defaults/main.yml` under `bind9_packages`
4. Use Jinja2 references in `argument_specs.yml` for non-mandatory field defaults (e.g., `default: "{{ bind9_packages }}"`) instead of hardcoding values

</domain>

<decisions>
## Implementation Decisions

### bind9_listen_ipv4 derivation
- Keep `bind9_listen_ipv4` explicit in host_vars — do NOT auto-derive from `network_interfaces[].ip4`. Explicit control over which interfaces BIND listens on.

### lab_id assertion strategy
- Remove `lab_id: 14` from `group_vars/all.yml` entirely
- Remove the assertion task in `config.yml` that checks `lab_id == bind9_derived_lab_id`
- The derived `bind9_derived_lab_id` remains the single source of truth for VPN identity

### argument_specs default style
- Use Jinja2 references (e.g., `default: "{{ bind9_packages }}"`) for non-mandatory fields in `argument_specs.yml` to stay DRY with `defaults/main.yml`

### the agent's Discretion
- Item 3 (removing bind-utils from base role) is straightforward — no ambiguity

</decisions>

<specifics>
## Specific Ideas

- `bind9_listen_ipv4` stays as-is in host_vars for primary-ns-01, secondary-ns-01, dns-01
- `lab_id` removal: delete line 8 (`lab_id: 14`) from `group_vars/all.yml`, remove the assertion block (lines 21-29) from `config.yml`
- `bind-utils` removal: remove the `bind-utils` entry from the package list in `base/tasks/install.yml` (line 27)
- `argument_specs.yml`: change hardcoded defaults to Jinja2 references for all non-mandatory fields that have corresponding defaults in `defaults/main.yml`

</specifics>

<canonical_refs>
## Canonical References

- `ansible/playbooks/roles/bind9/defaults/main.yml` — source of truth for default values
- `ansible/playbooks/roles/bind9/meta/argument_specs.yml` — validation schema referencing defaults
- `ansible/playbooks/roles/bind9/tasks/config.yml` — contains the lab_id assertion to remove
- `ansible/inventory/group_vars/all.yml` — contains `lab_id: 14` to remove
- `ansible/playbooks/roles/base/tasks/install.yml` — contains `bind-utils` to remove

</canonical_refs>