---
phase: quick-260506-i20-add-selinux-setype-attributes-to-bind9-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/playbooks/roles/bind9/tasks/config.yml
autonomous: true
requirements:
  - SEC-01
  - AUTO-01
must_haves:
  truths:
    - On SELinux-enforcing AlmaLinux hosts, bind9 configuration paths are created and rendered with file types that named can read without manual relabeling.
    - The dynamic zone state directory is labeled as writable named cache content so DDNS journals and transfer state can persist.
    - Re-running the bind9 role preserves correct file contexts through Ansible-native task attributes instead of a follow-up restorecon step.
  artifacts:
    - path: ansible/playbooks/roles/bind9/tasks/config.yml
      provides: Explicit SELinux setype attributes on every bind9 file and template task in the config phase.
      contains: "setype:"
  key_links:
    - from: ansible/playbooks/roles/bind9/tasks/config.yml
      to: ansible/playbooks/roles/bind9/defaults/main.yml
      via: bind9_conf_dir and bind9_conf_path determine which config directories and files must receive named_conf_t.
      pattern: "bind9_conf_(dir|path)"
    - from: ansible/playbooks/roles/bind9/tasks/config.yml
      to: ansible/playbooks/roles/bind9/defaults/main.yml
      via: bind9_dynamic_zone_dir determines which writable directory must receive named_cache_t.
      pattern: "bind9_dynamic_zone_dir"
---

<objective>
Add explicit SELinux file types to the bind9 config-phase filesystem tasks so AlmaLinux hosts running in enforcing mode keep correct named file contexts without post-task relabel work.

Purpose: Honor the locked SELinux labeling decisions for bind9 config assets and writable dynamic state while keeping the role idempotent.
Output: Updated `ansible/playbooks/roles/bind9/tasks/config.yml` with explicit `setype` values on every `file` and `template` task.
</objective>

<execution_context>
@/Users/j.huang.rj/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/j.huang.rj/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/quick/260506-i20-add-selinux-setype-attributes-to-bind9-r/260506-i20-CONTEXT.md
@ansible/playbooks/roles/bind9/defaults/main.yml
@ansible/playbooks/roles/bind9/tasks/config.yml

<interfaces>
From ansible/playbooks/roles/bind9/defaults/main.yml:
```yaml
bind9_conf_path: /etc/named.conf
bind9_conf_dir: /etc/named
bind9_zone_dir: /var/named
bind9_dynamic_zone_dir: /var/named/dynamic
```

From ansible/playbooks/roles/bind9/tasks/config.yml:
```yaml
- ansible.builtin.file: path: "{{ bind9_conf_dir }}"
- ansible.builtin.file: path: "{{ bind9_dynamic_zone_dir }}"
- ansible.builtin.template: dest: "{{ bind9_conf_path }}"
- ansible.builtin.template: dest: "{{ bind9_conf_dir }}/named.options.conf"
- ansible.builtin.template: dest: "{{ bind9_conf_dir }}/named.acl.conf"
```
These are the only existing `file` and `template` tasks in `config.yml`; the change should stay scoped to them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit SELinux setype labels to every bind9 config-phase file operation</name>
  <files>ansible/playbooks/roles/bind9/tasks/config.yml</files>
  <action>Update `ansible/playbooks/roles/bind9/tasks/config.yml` so every existing `ansible.builtin.file` and `ansible.builtin.template` task carries an explicit `setype` per the locked `setype scope` decision. Use `named_conf_t` for `{{ bind9_conf_dir }}`, `{{ bind9_conf_path }}`, and every rendered config file under `{{ bind9_conf_dir }}`; use `named_cache_t` for `{{ bind9_dynamic_zone_dir }}` per the locked `dynamic zone dir context` decision. Keep task names, ownership, group, mode, and notify behavior unchanged. Do not add a `restorecon` handler, `sefcontext` management, or any extra relabel step; the fix must remain Ansible-native and idempotent. Because `config.yml` currently does not create or template files under `{{ bind9_zone_dir }}`, do not invent new tasks solely to force `named_zone_t` into scope.</action>
  <verify>
    <automated>test "$(rg -c '^\s+setype:' ansible/playbooks/roles/bind9/tasks/config.yml)" -eq 5 &amp;&amp; test "$(rg -c 'setype: "named_conf_t"' ansible/playbooks/roles/bind9/tasks/config.yml)" -eq 4 &amp;&amp; test "$(rg -c 'setype: "named_cache_t"' ansible/playbooks/roles/bind9/tasks/config.yml)" -eq 1 &amp;&amp; ansible-playbook -i ansible/inventory/hosts.yml ansible/playbooks/bootstrap.yml --syntax-check</automated>
  </verify>
  <done>All five existing `file` and `template` tasks in `config.yml` declare the intended SELinux type, config assets use `named_conf_t`, the dynamic state directory uses `named_cache_t`, and the playbook still passes syntax validation without adding relabel side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bind9 role tasks → managed filesystem paths | Ansible definitions choose the SELinux labels applied to `/etc/named*` and `/var/named/dynamic` on the target host. |
| named daemon → SELinux-labeled files | The service can only read config and write dynamic state if the managed paths carry compatible labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-i20-01 | D | ansible/playbooks/roles/bind9/tasks/config.yml | mitigate | Apply `named_conf_t` to the config directory and rendered config files so SELinux does not block `named` from reading its configuration on enforcing hosts. |
| T-i20-02 | D | ansible/playbooks/roles/bind9/tasks/config.yml | mitigate | Apply `named_cache_t` to `bind9_dynamic_zone_dir` so DDNS journals and transfer state remain writable to `named`. |
| T-i20-03 | T | ansible/playbooks/roles/bind9/tasks/config.yml | mitigate | Use task-level `setype` attributes instead of ad hoc relabel commands so reruns stay declarative, reviewable, and idempotent. |
</threat_model>

<verification>
- `test "$(rg -c '^\s+setype:' ansible/playbooks/roles/bind9/tasks/config.yml)" -eq 5`
- `test "$(rg -c 'setype: "named_conf_t"' ansible/playbooks/roles/bind9/tasks/config.yml)" -eq 4`
- `test "$(rg -c 'setype: "named_cache_t"' ansible/playbooks/roles/bind9/tasks/config.yml)" -eq 1`
- `ansible-playbook -i ansible/inventory/hosts.yml ansible/playbooks/bootstrap.yml --syntax-check`
</verification>

<success_criteria>
- `ansible/playbooks/roles/bind9/tasks/config.yml` is the only modified file and every existing `file`/`template` task in it now declares a SELinux `setype`.
- `/etc/named.conf`, `/etc/named/`, and the rendered config fragments are assigned `named_conf_t`, while `/var/named/dynamic` is assigned `named_cache_t`.
- The solution does not add `restorecon` or other out-of-band relabel logic, and the playbook remains syntactically valid.
</success_criteria>

<output>
After completion, create `.planning/quick/260506-i20-add-selinux-setype-attributes-to-bind9-r/260506-i20-SUMMARY.md`
</output>
